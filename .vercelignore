# ============================================================
# 不部署到 Vercel 的檔案（仍會進 git，只是線上站台不會有）
# ============================================================

# 文章原始草稿：網站只讀 articles.json，不讀這個資料夾。
# 若讓它進入部署，草稿會變成公開網址（實測過 HTTP 200），
# 內容與正式文章幾乎相同，對索引是多餘的重複內容訊號。
文章/
